# 해시
```java
Map<String, Integer> m = new HashMap<>();

m.merge("Hello", 1, Integer::sum);

m.get("Hello")

m.keySet().stream().findFirst().get();
```

# 집합
```java
Set<Integer> s = new HashSet<>();

s.add(1);

s.contains(1);
s.isEmpty();
s.remove(1);
```

# 리스트
```java
List<String> list = new ArrayList<>();

list.add("Hello");
list.indexOf("Hello");

```

# 배열
```java
boolean[] vst = new boolean[10];
```